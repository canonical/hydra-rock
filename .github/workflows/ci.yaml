name: CI

on:
  push:
    branches:
     - "main"
    paths:
      - "rockcraft.yaml"
      - ".github/workflows/**.yaml"
  pull_request:
    branches:
     - "*"
  workflow_dispatch:

jobs:
  build:
    uses: canonical/identity-team/.github/workflows/_rock-build.yaml@59d4d2782bcb8fc2deea2aa08442e4c8603f2689 # v1.11.2
    with:
      structure-tests-enabled: true
  gh-publish:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: build
    uses: canonical/identity-team/.github/workflows/_rock-gh-publish-third-party.yaml@59d4d2782bcb8fc2deea2aa08442e4c8603f2689 # v1.11.2
    with:
      rock: ${{ needs.build.outputs.rock }}
  oci-publish:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: build
    uses: canonical/identity-team/.github/workflows/_rock-oci-publish.yaml@59d4d2782bcb8fc2deea2aa08442e4c8603f2689 # v1.11.2
    with:
      edge-release: false
      eol-stable-release: "3 month"
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
  scan:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: gh-publish
    uses: canonical/identity-team/.github/workflows/_rock-scan.yaml@59d4d2782bcb8fc2deea2aa08442e4c8603f2689 # v1.11.2
    with:
      image: ${{ needs.gh-publish.outputs.image }}
